<div class="container">
  <div class="row">
    <% @books.each do |book| %>
      <div class="col s4 m6"></div>
      <div class="card">
        <span class="card-title"><%= book.title %></span>
      </div>
    <% end %>
  </div>
</div>
